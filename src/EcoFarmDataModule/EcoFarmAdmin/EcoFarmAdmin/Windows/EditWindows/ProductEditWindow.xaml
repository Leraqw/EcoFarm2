<Window x:Class="EcoFarmAdmin.EditWindows.ProductEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:m="clr-namespace:EcoFarmAdmin.Domain"
        xmlns:vm="clr-namespace:EcoFarmAdmin.ViewModels"
        mc:Ignorable="d"
        Title="ProductEditWindow" Height="450" Width="800">
    <Window.DataContext>
        <vm:ProductEditViewModel />
    </Window.DataContext>

    <Grid Margin="25, 75, 25, 50">
        <Grid.RowDefinitions>
            <RowDefinition Height="2*" />
            <RowDefinition Height="2*" />
            <RowDefinition Height="2*" />
            <RowDefinition Height="1*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="4*" />
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>

        <TextBlock Text="Название" TextAlignment="Right" Grid.Row="0" Grid.Column="0" />
        <TextBlock Text="Описание" TextAlignment="Right" Grid.Row="1" Grid.Column="0" />
        <TextBlock Text="Цена" TextAlignment="Right" Grid.Row="2" Grid.Column="0" />

        <TextBox Height="25" VerticalAlignment="Top" Text="{Binding DevObject.Title}" Grid.Row="0" Grid.Column="1" />
        <TextBox Height="25" VerticalAlignment="Top" Text="{Binding DevObject.Description}" Grid.Row="1" Grid.Column="1" />
        <TextBox Height="25" VerticalAlignment="Top" Text="{Binding DevObject.Price}" Grid.Row="2" Grid.Column="1" />

        <StackPanel HorizontalAlignment="Center" Orientation="Horizontal" Grid.Column="0" Grid.ColumnSpan="3" Grid.Row="3">
            <Button Margin="10, 0, 10, 15" IsDefault="True">ОК</Button>
            <Button Margin="10, 0, 10, 15" IsCancel="True">Отмена</Button>
        </StackPanel>

    </Grid>
</Window>